{
  "name": "titan-browser-ui",
  "version": "0.4.2",
  "private": true,
  "scripts": {
    "build": "npm run build:ui && npm run build:web-scripts",
    "build:ui": "tsc -p tsconfig.json",
    "build:web-scripts": "tsc -p web-scripts/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
